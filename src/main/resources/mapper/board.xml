<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.happyhouse.dao.BoardDao">
	<!-- 게시글 전체 조회 -->
	<select id="selectBoard" resultType="com.ssafy.happyhouse.model.BoardDto">
		select no, title, writer, content, regtime
		  from vue_board 
		 order by no
	</select>
	
	<!-- 게시글 상세 조회 -->
	<select id="selectBoardByNo" resultType="com.ssafy.happyhouse.model.BoardDto">
		select no, title, writer, content, regtime,password
		  from vue_board 
		 where no = #{no}
	</select>

	<!-- 게시글 등록 -->
	<insert id="insertBoard">
		insert into vue_board(
			title, writer, content,password
		) values (
			#{title}, #{writer}, #{content},#{password}
		)
	</insert>
	
	<!-- 게시글 수정 -->
	<update id="updateBoard">
		update vue_board 
		   set title = #{title}, 
		   	   writer = #{writer}, 
		   	   content = #{content}
		 where no = #{no}
	</update>

	<!-- 게시글 삭제 -->
	<delete id="deleteBoard">
		delete 
		  from vue_board 
		 where no = #{no}
	</delete>
</mapper>

